server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://root:root@localhost:8761/eureka}
  instance:
    hostname: localhost
spring:
  application:
    name: ticket-booking-service